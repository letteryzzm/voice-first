# Pi 英语语音教练实现待办（审查版）

- 状态：仅规划，未开始编码
- 日期：2026-04-27
- 目的：在开始实现前，对范围、架构、交互、工具边界和笔记约定逐项审查，避免需求理解不一致导致返工

## 1. 审查目标

本文件不是开发任务分配表，而是实现前的对齐清单。

使用方式：

- 先逐项确认“产品边界”是否正确。
- 再确认“实现边界”是否合理。
- 最后再进入技术设计和编码。

本阶段明确不做的事：

- 不写实现代码
- 不接入后台常驻进程
- 不扩展到通用桌面助手
- 不提前引入多工具系统

## 2. 第一层审查：范围是否对齐

以下内容需要先确认，任何一项不清楚，都不应开始编码：

- [ ] V1 的产品目标是“英语语音教练 + 英语笔记读写”，不是“通用语音 Agent”。
- [ ] V1 的运行方式是“手动启动程序、手动录音、手动停止、等待处理、播放结果”。
- [ ] V1 是 CLI 交互，不做 GUI，不做悬浮窗。
- [ ] V1 不做后台常驻，不做持续监听。
- [ ] V1 不做全电脑记忆，不做系统级上下文感知。
- [ ] V1 只保留一个工具：`bash`。
- [ ] V1 的笔记存储范围是本地英语笔记目录，而不是任意目录。
- [ ] V1 的主要用户就是当前开发者本人，不按多人产品设计。

如果以上任意一点需要改动，应先改 `PRD.md`，再继续后续设计。

## 3. 第二层审查：笔记边界是否对齐

当前已知笔记根目录为：

- `/Users/lettery/Documents/zzm/note/English`

需要确认的点：

- [ ] 该目录就是 V1 唯一的英语学习笔记根目录。
- [ ] V1 对英语笔记的读写默认只发生在这个目录内。
- [ ] 当前目录中的已有文件应视为用户资产，默认不做破坏性修改。
- [ ] V1 写入策略以“追加”或“新建文件”为主，而不是覆盖。
- [ ] V1 是否要固定生成以下文件：
  - `vocab.md`
  - `corrections.md`
  - `sessions/`
- [ ] 如果不固定结构，是否允许 Agent根据现有目录内容自行适配。

需要你审查的风险点：

- 如果笔记结构不提前约定，后续 Prompt、`bash` 策略和写入模板都可能反复变更。
- 如果写入策略不提前约定，最容易出现的错误就是“写对了内容，写错了地方”。

## 4. 第三层审查：Pi 集成边界是否对齐

这里最容易出问题，所以单独列出来。

当前建议的边界是：

- Pi 的通用 `Agent Core` 继续负责：
  - 对话上下文
  - System Prompt
  - Tool Calling
  - Agent Loop
- 语音运行时负责：
  - 开始录音
  - 停止录音
  - 调用 STT
  - 调用 TTS
  - 播放结果
  - CLI 状态管理

需要确认的点：

- [ ] 你说的“接进 Pi 核心”，实际是“建立在 Pi Agent 能力之上”，而不是把麦克风和扬声器逻辑直接写进最底层 `packages/agent`。
- [ ] V1 是否接受“Pi Core 保持通用，语音能力放在上层包中”的架构。
- [ ] 语音相关代码最终放在哪个包，需要先决定：
  - 方案 A：`packages/coding-agent`
  - 方案 B：新建 `packages/voice-agent`
- [ ] V1 是否接受先做“单次语音请求的 Runtime”，而不是一步做成“持续通话模式”。

需要你审查的风险点：

- 如果直接修改最底层通用 Core，后续会把整个仓库架构绑死。
- 如果不先定义 Pi Core 和语音层的边界，后面所有模块命名、依赖方向和状态流都会变乱。

## 5. 第四层审查：交互流程是否对齐

V1 当前定义的标准流程是：

1. 运行程序
2. 进入等待状态
3. 开始录音
4. 停止录音
5. 执行转写
6. 执行 Agent 推理
7. 如有需要执行 `bash`
8. 得到文本回复
9. 转成语音
10. 播放输出
11. 回到等待状态

需要确认的点：

- [ ] 这就是 V1 的唯一交互主流程。
- [ ] V1 不要求实时流式字幕。
- [ ] V1 不要求用户说话时实时打断播报。
- [ ] V1 不要求“边说边想边播”的通话式体验。
- [ ] V1 是否采用如下状态集合：
  - `idle`
  - `recording`
  - `transcribing`
  - `thinking`
  - `executing bash`
  - `speaking`
  - `done`
  - `error`
- [ ] CLI 控制方式是否采用以下二选一：
  - `r / s / q`
  - 回车开始，再次回车结束

需要你审查的风险点：

- 如果交互流程不冻结，后面 STT、TTS、状态机和日志展示都会反复调整。
- 如果现在就想兼容持续录音模式，会显著拉高 V1 复杂度。

## 6. 第五层审查：英语教练角色是否对齐

V1 的 Agent 角色不是“秘书”，也不是“写代码助手”，而是“英语教练”。

需要确认的点：

- [ ] 默认优先用英语和用户对话。
- [ ] 用户用中文问语法、词义、表达差异时，可以用中文解释。
- [ ] 默认采用“轻纠错”，避免每句都打断。
- [ ] 用户要求严格纠错时，再提高纠错密度。
- [ ] 当用户请求记笔记时，Agent 应把本轮学习结果沉淀成 Markdown。
- [ ] Agent 的回答目标是“帮助学习”，不是“展示模型能力”。

需要你审查的风险点：

- 如果角色不收敛，后面会出现“会聊天但不适合学习”的偏差。
- 如果纠错策略不明确，体验要么太啰嗦，要么太弱。

## 7. 第六层审查：`bash` 工具边界是否对齐

V1 只保留 `bash`，这意味着必须提前把边界讲清楚。

需要确认的允许行为：

- [ ] 搜索英语笔记目录内的 Markdown 内容。
- [ ] 读取英语笔记目录内的 Markdown 文件。
- [ ] 在英语笔记目录内创建新 Markdown 文件。
- [ ] 向英语笔记目录内的文件追加内容。
- [ ] 做简单文本处理，如拼接、提取、格式化。
- [ ] 在当前项目目录内处理与本产品直接相关的 Markdown 或代码文件。

需要确认的禁止或限制行为：

- [ ] 不做与产品无关的系统操作。
- [ ] 不默认删除文件。
- [ ] 不默认覆盖用户已有笔记。
- [ ] 不允许无边界写入任意目录。
- [ ] 不扩展到浏览器、桌面 GUI、系统控制等无关自动化。

需要你审查的风险点：

- 如果 `bash` 没有边界，V1 很容易从“英语学习工具”变成“不受控的本地 Agent”。
- 如果工作目录策略不清晰，后面会出现路径错误和误写风险。

## 8. 第七层审查：STT / TTS 选择是否需要先定

这部分暂时不要求你现在就定死 Provider，但要确认是否要在编码前拍板。

STT 侧待确认：

- [ ] V1 是否继续复用当前原型中的 STT 路线。
- [ ] V1 是否只做整段转写，不做流式部分转写。
- [ ] V1 是否要求较好支持中英混合输入。

TTS 侧待确认：

- [ ] V1 是否继续复用当前原型中的 TTS 路线。
- [ ] V1 是否接受“先生成完整文本，再统一播报”。
- [ ] V1 是否需要对回复长度做限制，避免播报过长。

需要你审查的风险点：

- 如果 Provider 不提前定，后面接口抽象和错误处理会重复修改。
- 如果回复长度策略不定，TTS 体验会不稳定。

## 9. 第八层审查：配置与安全是否对齐

这是必须先处理的问题，不然后面所有实现都不干净。

需要确认的点：

- [ ] 原型中的硬编码 API Key 在正式实现前必须移除。
- [ ] 所有 Provider 配置都改走环境变量。
- [ ] 仓库中提供 `env.example` 作为配置模板。
- [ ] 本地开发使用 Git 忽略的 `.env`。
- [ ] `.env` 不进入版本控制。
- [ ] 如果仓库当前还没有 `.gitignore`，需要补上 `.env` 忽略规则。
- [ ] 缺少 Key 时程序必须明确失败，而不是静默退化。
- [ ] 需要明确哪些字段放进 `env.example`，哪些只存在于本地 `.env`。

需要你审查的风险点：

- 如果把原型里的密钥写法直接带入新实现，会留下明显安全问题。
- 如果配置策略不统一，后续调试成本很高。
- 如果 `env.example` 字段定义不稳定，后面脚本、文档和启动方式会反复修改。

## 10. 第九层审查：错误处理是否对齐

需要明确哪些错误在 V1 必须给清晰反馈：

- [ ] 麦克风初始化失败
- [ ] 录音失败
- [ ] 转写失败
- [ ] Agent 推理失败
- [ ] `bash` 执行失败
- [ ] TTS 生成失败
- [ ] 音频播放失败

需要确认的策略：

- [ ] 出错时终端必须能看懂，不允许静默卡住。
- [ ] 笔记写入失败时，是否仍然可以返回文本结果。
- [ ] 播放失败时，是否仍然保留终端文本输出作为兜底。
- [ ] 程序退出时，录音与播放资源必须被正确释放。

## 11. 编码前必须拍板的事项

以下事项在进入编码前必须明确：

- [ ] V1 范围确认
- [ ] 英语笔记目录边界确认
- [ ] Pi 集成边界确认
- [ ] 语音 Runtime 所在包确认
- [ ] CLI 主流程确认
- [ ] 英语教练 Prompt 方向确认
- [ ] `bash` 使用边界确认
- [ ] 笔记写入策略确认
- [ ] STT 路线确认
- [ ] TTS 路线确认
- [ ] 配置和密钥策略确认（`env.example` + 本地 `.env` + Git 忽略）

## 12. 建议实现顺序（仍不写代码）

在进入编码前，建议按以下顺序继续产出文档：

- [ ] 先把本文件审查通过。
- [ ] 基于本文件产出一份技术设计文档。
- [ ] 在技术设计文档中明确包结构、模块边界、状态机和 Provider 接口。
- [ ] 再单独产出一版英语教练 System Prompt 草稿。
- [ ] 再单独产出一版 `bash` 操作策略与样例命令约定。
- [ ] 上述文档稳定后，再开始编码。

## 13. 明确延后到 V2 的内容

以下内容明确不进入 V1：

- [ ] 后台常驻进程
- [ ] 持续运行的录音循环
- [ ] 实时打断播报
- [ ] 连续通话模式
- [ ] 热键唤醒
- [ ] GUI 窗口或悬浮层
- [ ] 全电脑记忆
- [ ] 通用桌面自动化

## 14. 审查结论填写区

这一节用于你审查后填写，不是当前内容的一部分。

建议你审查时至少给出以下结论：

- [ ] 哪些内容完全同意
- [ ] 哪些内容需要修改
- [ ] 哪些内容还不够具体
- [ ] 哪些内容你认为有架构风险
- [ ] 哪些内容必须在编码前再补一份独立文档
