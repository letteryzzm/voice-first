# Pi 英语语音教练 PRD

- 版本：V1
- 日期：2026-04-27
- 状态：草稿
- 平台：macOS 本地运行
- 产品形态：基于 Pi Agent 能力的 CLI 优先语音交互程序
- 英语笔记根目录：`/Users/lettery/Documents/zzm/note/English`

## 1. 产品定义

Pi 英语语音教练是一个基于现有 Pi Agent Runtime 的语音优先英语学习 Agent。

用户通过语音与系统交互。系统录制一段语音，将语音转成文字，把文字交给英语教练 Agent 处理；如有需要，Agent 通过受限的 `bash` 工具读取或写入本地 Markdown 笔记；最后系统将文本结果转成语音并播放。

V1 的范围刻意收窄为：

- 单用户本地使用
- 单个 CLI 进程
- 单次录音 / 停止 / 请求 / 响应流程
- 只保留一个工具：`bash`
- 只面向一个存储域：本地英语笔记，以及与之相关的 Markdown / 文件 / 代码处理

## 2. 产品目标

V1 目标如下：

- 让用户可以通过语音进行英语练习。
- 让 Agent 能读取用户已有的英语笔记。
- 让 Agent 能把有价值的学习结果写回笔记目录。
- 复用 Pi 的 Agent Loop 和 Tool Calling 能力，而不是再造一套无关核心。
- 先把端到端闭环稳定跑通，再考虑后台常驻与持续交互。

## 3. 非目标

V1 不包含以下内容：

- 持续监听
- 后台守护进程或菜单栏应用
- 热词唤醒
- 全电脑记忆或系统级上下文感知
- `bash` 之外的多工具系统
- GUI 窗口或悬浮层
- 复杂权限中心
- 云同步或移动端同步
- 流式打断或全双工通话模式

## 4. 目标用户

主要用户为：

- 仓库所有者本人
- 以中文为母语的英语学习者
- 希望通过语音练习英语
- 希望把学习结果沉淀到本地 Markdown 笔记中
- 能接受 V1 采用 CLI 交互方式

## 5. 用户问题

当前要解决的问题：

- 现有口语练习工具不了解用户自己的英语笔记。
- 现有笔记工具可以存内容，但不是好的语音英语教练。
- 从口语练习切换到记笔记仍然是手动操作，会打断学习流。
- 现有原型更像通用语音 Agent，而不是面向英语学习的产品。

## 6. 产品价值主张

这个产品把三件事收敛到一个闭环里：

- 语音英语交互
- 英语教练式推理
- 对本地英语笔记的直接读写

重点不是通用桌面控制，而是把“个人英语笔记”变成“可对话的学习记忆”。

## 7. V1 范围

### 7.1 包含内容

- 从终端手动运行程序
- 手动开始录音
- 手动结束录音
- 将录音送入 STT
- 将转写文本送入基于 Pi 的 Agent
- 允许 Agent 使用受限的 `bash`
- 读取和写入 Markdown 笔记
- 生成最终文本回复
- 将最终文本转为语音并播放
- 在终端打印交互状态
- 在单次运行期间保留短会话上下文

### 7.2 不包含内容

- 实时流式转写 UI
- 实时打断播报
- 连续通话式多轮交互
- 常驻后台进程
- 自动恢复会话
- 复杂间隔复习系统
- 多 Agent 协同

## 8. 主要使用场景

### 8.1 英语口语练习

示例：

- “Let's practice English about technology.”
- “Please talk to me in English and correct major mistakes only.”
- “Is this sentence natural?”

预期行为：

- Agent 主要用英语回应
- 保持对话流畅
- 在合适的时候给出简短纠错
- 除非用户要求，不给过长解释

### 8.2 中文辅助英语表达

示例：

- “这句话更自然的英文怎么说？”
- “adolescence 和 teenage years 有什么区别？”
- “帮我把这个表达写得更像母语者。”

预期行为：

- 用户用中文问时，可以用中文解释
- 给出更自然的英文表达
- 必要时给一个简短示例句

### 8.3 读取已有笔记

示例：

- “读一下我关于 technology adolescence 的笔记。”
- “这个词我之前记过什么？”
- “总结一下我英语笔记目录里的这篇 Markdown。”

预期行为：

- 在笔记根目录下搜索相关内容
- 读取相关文件
- 给出总结或有依据的回答
- 尽量基于实际文件内容回答

### 8.4 写入新的学习笔记

示例：

- “把这个单词记到我的词汇笔记里。”
- “把刚才纠正后的句子写到 corrections.md。”
- “为今天这次口语练习生成一个总结。”

预期行为：

- 写入到允许的笔记根目录
- 优先采用追加或新建方式
- 除非用户明确要求，否则不做破坏性覆盖

## 9. 用户流程

V1 交互流程如下：

1. 用户运行程序。
2. 系统初始化 Pi Runtime、STT 适配层、TTS 适配层和录音器。
3. 系统进入 `idle` 状态。
4. 用户触发开始录音。
5. 系统进入 `recording` 状态。
6. 用户停止录音。
7. 系统进入 `transcribing` 状态。
8. STT 返回转写文本。
9. 系统进入 `thinking` 状态，并把文本发送给 Agent。
10. Agent 在需要时执行受限的 `bash`。
11. 系统接收最终文本结果。
12. 系统进入 `speaking` 状态。
13. TTS 生成音频并播放。
14. 系统进入 `done`，随后回到 `idle`，等待下一轮请求。

## 10. 交互要求

### 10.1 CLI 控制

V1 控制方式可以保持极简。可接受方案之一：

- `r`：开始录音
- `s`：停止录音
- `q`：退出程序

另一个可接受方案：

- 按一次回车开始录音
- 再按一次回车停止录音

PRD 不强制具体按键，但 V1 必须采用显式、手动控制。

### 10.2 运行状态

终端应清晰显示以下状态：

- `idle`
- `recording`
- `transcribing`
- `thinking`
- `executing bash`
- `speaking`
- `done`
- `error`

### 10.3 输出形式

每一轮交互，终端至少要展示：

- 用户转写文本
- Agent 的文本回复
- 是否调用了 `bash`
- 最终完成状态

## 11. 英语教练角色要求

这个 Agent 不是通用助手，而是英语教练。

要求如下：

- 在口语练习场景下默认优先用英语回答
- 当用户用中文询问语法、词义、表达区别时，可以改用中文解释
- 回答目标是帮助学习，而不是表现得像通用客服
- 如果用户英文不自然，应尽量提供：
  - 修改后的表达
  - 简短原因
  - 一个简洁示例（如有必要）
- 不要过度纠错，以免打断对话流
- 用户要求读写笔记时，利用本地笔记作为长期学习记忆

## 12. 工具能力要求

### 12.1 V1 工具集

V1 只要求一个工具：

- `bash`

V1 不要求单独暴露 `read_file`、`write_file` 或 GUI 工具。

### 12.2 Bash 使用策略

虽然只暴露 `bash`，但产品行为必须对它做限制。

允许的用途：

- 读取 Markdown 文件
- 搜索笔记内容
- 创建 Markdown 文件
- 追加笔记内容
- 做简单的文本/文件处理
- 视需要处理与当前产品直接相关的本地 Markdown 或代码文件

不允许或应强烈限制的用途：

- 与产品无关的系统操作
- 未经明确要求的破坏性删除
- 超出允许范围的任意写入
- 超出产品范围的广泛桌面自动化

### 12.3 目录边界

批准的英语笔记根目录：

- `/Users/lettery/Documents/zzm/note/English`

产品工作目录可以包含当前项目仓库，但与英语笔记有关的操作默认应指向这个笔记根目录。

## 13. 笔记存储要求

当前笔记根路径是一个目录，不是单个文件。

V1 应把它视为用户的英语知识库。

### 13.1 最低支持内容

系统至少应支持：

- 读取笔记根目录下已有的 `.md` 文件
- 在需要时新建 `.md` 文件
- 向已有文件追加新的学习内容

### 13.2 建议的初始文件约定

V1 可以创建或使用如下文件：

- `vocab.md`
- `corrections.md`
- `sessions/YYYY-MM-DD-HHMM.md`

这是建议，不是硬性约束。如果用户已有更合适的目录结构，实现可以适配。

### 13.3 推荐写入策略

V1 推荐行为：

- 追加优先于覆盖
- 做会话总结时优先创建新的 session 文件
- 尽量保持用户原有笔记内容不被破坏

## 14. 系统架构要求

产品要接入 Pi，但不要把底层音频设备逻辑直接耦合到最通用的 Agent Core。

建议拆成以下几层：

### 14.1 语音运行时层

职责：

- 控制录音开始与结束
- 采集音频
- 播放音频
- 管理单轮语音请求的状态切换
- 负责 CLI 交互

### 14.2 STT / TTS 适配层

职责：

- 屏蔽具体 STT Provider 差异
- 屏蔽具体 TTS Provider 差异
- 向语音运行时提供稳定接口

### 14.3 Pi Agent 层

职责：

- 提供 System Prompt
- 管理会话上下文
- 负责 Tool Calling
- 生成最终回复
- 体现英语教练角色行为

### 14.4 文件操作层

职责：

- 执行受限 `bash`
- 尽可能约束工作目录和可操作路径
- 记录文件操作，便于排查和理解

## 15. 与 Pi 的集成策略

产品要求：

- V1 应建立在 Pi 的 Agent 能力之上。

工程要求：

- 不要把麦克风、录音器、扬声器等逻辑直接塞进最通用的 `packages/agent` Core。
- 应在 Pi Agent Core 之上增加一层面向语音的 Runtime。

推荐实现位置：

- 方案 A：扩展 `packages/coding-agent`
- 方案 B：新建 `packages/voice-agent`

原因：

- 保持 Pi Core 的通用性
- 避免通用 Agent Loop 与设备级音频逻辑强耦合
- 让 V2 的演进路径更清晰

## 16. 功能需求

### 16.1 录音

- 用户可以手动开始录音
- 用户可以手动停止录音
- 录音器返回当前一轮的音频数据
- 麦克风不可用时，系统必须给出明确错误

### 16.2 语音转文字

- 系统将单轮录音转成文本
- V1 不要求实时流式部分转写
- 系统应足够支持英语、中文或中英混合输入

### 16.3 Agent 推理

- 转写文本送入基于 Pi 的 Agent
- Agent 在当前进程中保留会话上下文
- Agent 使用英语教练专用提示词
- 当需要读写笔记时，Agent 可以调用 `bash`

### 16.4 文字转语音

- Agent 最终文本回复被转成语音
- V1 可以等待完整文本生成后再开始 TTS
- 播放失败时，终端必须清楚报错

### 16.5 笔记读写

- Agent 可以搜索和读取 Markdown 笔记内容
- Agent 可以把学习记录追加到笔记文件中
- Agent 可以创建新的 session 笔记
- 默认避免破坏性修改

## 17. 非功能需求

- 程序启动后应在几秒内进入可用状态
- 单轮语音请求应以稳定完成为先，不强求极致性能
- 终端状态输出必须清楚可理解
- 程序退出时必须正确释放资源
- 出错时不能静默卡死
- 文件操作应尽可能受限且可追踪
- 配置管理采用 `env.example` + 本地 `.env` 形式
- 本地 `.env` 必须加入 Git 忽略，不进入版本控制
- 仓库中只提交示例配置，不提交真实密钥

## 18. V1 成功标准

若满足以下条件，可认为 V1 成立：

- 程序可以从终端启动
- 用户可以手动开始和停止录音
- 录音可以成功转写
- 转写结果可以交给基于 Pi 的 Agent 处理
- Agent 能以英语教练身份回答
- Agent 能通过 `bash` 读取英语笔记目录中的文件
- Agent 能通过 `bash` 写入新的 Markdown 学习记录
- 最终回答可以被播报出来
- 终端能清楚反映状态变化和错误信息

## 19. 主要风险与约束

### 19.1 架构风险

如果把音频逻辑直接塞进 Pi 最底层通用 Core，后续维护成本会显著上升。

缓解方式：

- 把语音 Runtime 放在 Agent Core 之上

### 19.2 工具安全风险

只暴露 `bash` 灵活，但风险也高。

缓解方式：

- 用 Prompt 限制 Agent 行为
- 用工作目录和目标路径约束文件操作
- 默认偏向追加 / 新建，而不是覆盖 / 删除

### 19.3 配置安全风险

如果把原型中的硬编码密钥或本地私有配置直接带入仓库，会留下明显安全问题。

缓解方式：

- 统一采用 `env.example` 提供配置模板
- 本地开发机使用 `.env`
- `.env` 必须被 Git 忽略
- 禁止在代码和脚本中硬编码真实 API Key

### 19.4 产品跑偏风险

项目可能重新膨胀成通用桌面助手。

缓解方式：

- V1 严格聚焦于英语学习与笔记记忆

### 19.5 交互复杂度风险

如果过早加入持续监听、流式打断、通话模式，会显著拖慢落地速度。

缓解方式：

- V1 维持单轮、手动控制的录音请求流程

## 20. V2 方向

V2 可以考虑加入：

- 常驻后台 Runtime
- 更快的连续多轮交互
- 更自然的通话式语音交互
- 中途打断播报
- 自动语音端点检测
- 更好的笔记组织与复习流程
- 更强的文件操作安全约束

## 21. 待定实现决策

以下仍属于实现决策，不阻塞 PRD：

- V1 最终保留哪套 STT Provider
- V1 最终保留哪套 TTS Provider
- 第一个语音 Runtime 是放到 `packages/coding-agent` 还是新建 `packages/voice-agent`
- CLI 的最终按键方案
- 笔记文件命名约定是否固定

以下已明确，不再作为待定项：

- 配置采用 `env.example` 模板文件
- 本地开发使用 `.env`
- `.env` 必须被 Git 忽略

## 22. 附录：初始提示词方向

第一版英语教练 System Prompt 应至少约束以下行为：

- 你是英语口语与表达教练，不是通用助手
- 默认优先用英语对话
- 用户用中文问时可用中文解释
- 默认只纠正最重要的问题，除非用户要求严格纠错
- 写笔记时请输出结构清晰的 Markdown
- 不要执行与英语学习无关的 Shell 操作
- 所有行为都应聚焦在用户的英语学习目标内
